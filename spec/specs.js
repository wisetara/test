/*describe("fibonacci", function(number) {
  it ("returns the first number in the fibonacci sequence", function() {
    fibonacci(1).should.equal(0);
  });
  it ("returns the second number in the fibonacci sequence", function() {
    fibonacci(2).should.equal(1);  
  });
});

describe("fibAdding", function(factor1, factor2) {
  it ("adds two numbers and stores the result in an array", function() {
    fibAdding(1,2).should.eql([3]);
  });
});  

describe("fibAccess", function(position) {
  it ("returns the number in the entered position of the fib sequence", function() {
    fibAccess(9).should.equal(21);
  });
  it ("returns a pirate alert", function() {
    fibAccess(22).should.equal(alert);
  });
});*/

describe("doTheDamnThang", function(position) {
  it ("returns the value in the selected placeholder in the fibonacci sequence array", function() {
    doTheDamnThang(9).should.equal(21);
  });
});  

